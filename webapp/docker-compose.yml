version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: monitoring-redis
    ports:
      - "6379:6379"

  web:
    build: .
    container_name: monitoring-app
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    depends_on:
      - redis